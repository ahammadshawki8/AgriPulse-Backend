# Lightweight Requirements for Render Deployment
# Backend calls HuggingFace Space for AI detection

# Web Framework
Flask==3.0.0
flask-cors==4.0.0
Flask-SQLAlchemy==3.1.1
gunicorn==21.2.0

# Database (updated for Python 3.11+ compatibility)
psycopg2-binary==2.9.10

# Image Processing
Pillow>=10.2.0

# API Client for HuggingFace Space
gradio_client==2.2.0

# Utilities
python-dotenv==1.0.0
Werkzeug==3.0.1

# NO HEAVY DEPENDENCIES!
# We're using HuggingFace Space, so we DON'T need:
# - transformers (~2GB)
# - torch (~1GB)
# - torchvision (~500MB)
# This keeps deployment under 512MB RAM limit!
